import Vue from 'vue'
import {
    Popup, 
    cell,
    NavBar,
    Toast,
    Icon,
    Swipe, 
    SwipeItem,
    Lazyload,
    PullRefresh,
    Loading,
    Form,
    Field,
    Button,
    Search,
    Tabbar, 
    TabbarItem,
    Tab, 
    IndexBar, 
    IndexAnchor,
    Tabs
} from 'vant'
  
Vue.use(Popup).use(cell).use(NavBar).use(Toast).use(Icon).use(Swipe).use(SwipeItem).use(Lazyload).use( PullRefresh).use(Loading).use(Form).use(Field).use(Button).use(Search).use(Tabbar).use(TabbarItem).use(Tab).use(IndexAnchor).use(IndexBar).use(Tabs)
    